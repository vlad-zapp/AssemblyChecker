<Window x:Class="AsmChecker.ReportViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Width="800" Height="600">

    <Window.Resources>
        <DataTemplate x:Key="header_template">
            <StackPanel Orientation="Horizontal">
                <Image Source="{Binding Path=TypeImage}" Width="16" Height="16" HorizontalAlignment="Left"></Image>
                <TextBlock Margin="5,0,0,0" Text="{Binding Path=Name}"></TextBlock>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <TreeView Name="treeView1">
        </TreeView>
    </Grid>
</Window>